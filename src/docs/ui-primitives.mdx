import { Meta } from '@storybook/blocks';

<Meta title="Docs/UI Primitives" />

# UI primitives

These components are used across the app to keep the design calm and accessible:

- Large tap targets (minimum 44×44)
- Clear semantic colors
- Keyboard-visible focus rings
- Screen-reader labels where appropriate

See the stories under **UI/** for interactive examples.
